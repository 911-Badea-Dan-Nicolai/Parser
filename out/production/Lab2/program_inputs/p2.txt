var
	int a
	int i
	int divisors

start
	read a
	i <- a
	divisors <- 0

	while i > 0 run
		if a % i = 0
			divisors <- divisors + 1
		i <- i - 1

	if divisors = 2
		write "prime"
	otherwise
		write "not prime"
finish