var
	int abc123 (here is an error as we don't allow identifiers to contain digits)
	int b
	int c
	int max

start
	a <- 5
	b <- 7
	c <- 09 (here is an error because we don't allow numbers to start with 0)

	if a > b then
		max <- a
		if a < c then
			max <- c
	otherwise
		max <- b
		if b < c then
			max <- c
finish
